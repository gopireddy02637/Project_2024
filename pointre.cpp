// Online C compiler to run C program online
#include <stdio.h>
#include <stdlib.h>
int main() {
    int * gopi = (int*)malloc(sizeof(int));
    free(gopi);
    gopi=NULL;
    printf("Hello world");
    //when we create setion in heap we create free from memory but memeory
    return 0;
    /*
    nan is not a number if that is not in float then that is not a numer -2
    */
}